https://healthcost-eight.vercel.app

# 비급여진료비탐색서비스

`npm install` 후 `npm run dev`로 로컬에서 프론트 확인 (`/api/search`는 배포 환경에서만 동작).
데이터 갱신: `.env.local`에 `HIRA_API_KEY` 채운 뒤 `npm run fetch:data`.
git push 시 Vercel에 자동 배포.
