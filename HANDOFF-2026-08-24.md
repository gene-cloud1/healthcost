# 인수인계 — 2026-08-24 세션에서 다음 세션으로

> 이 파일은 새 세션 시작용 브리핑이다. 다 읽고 나서 작업 시작할 것.
> 작업 끝나면(강남구 전체 수집 완료되면) 이 파일은 지워도 된다 — 영구 문서 아님.

## 지금 하려는 일
`node scripts/fetch-hira-data.mjs --all 강남구` 재개. 강남구 전체(중복 제거 기준 실제
기관 수는 476보다 많을 수 있음 — 아래 "알아낸 것 5" 참고) 비급여 항목을 캡 없이 수집.

이미 있는 데이터(강남구 681건 캡드/서초구 580/송파구 526, 총 1787건, 전부 커밋됨)는
안전하다. 이번 작업은 강남구 것만 전체로 다시 갈아끼운다.

## 오늘 알아낸 것 (반드시 알고 시작할 것)

**1. HIRA `nonPaymentDamtInfoService`는 원래 느리고 불안정하다.**
- 정상 응답도 1~60초 걸림. 가끔 HTTP 200인데 `resultCode:99`
  (`weblogic...ResourceLimitException`, 서버 커넥션 풀 고갈) 또는 `resultCode:05`
  (SERVICETIMEOUT)를 반환한다. 우리 쪽 문제가 아니라 HIRA 서버 자체 문제.
- `scripts/fetch-hira-data.mjs`의 `callApi()`는 이미 이 두 경우 다 감지해서 재시도(5회,
  3초 간격)한다. **이 부분은 이미 고쳐져 있으니 또 만들지 말 것.**

**2. 절대 저장 없이 몇 시간을 날리는 구조로 되돌리지 말 것.**
- 예전엔 "구 하나 통째로 끝나야 저장"이었는데, opencode가 4시간씩 두 번 돌다 프로세스가
  죽어서 (아마 opencode 자체의 내부 실행시간 제한) 저장을 한 번도 못 하고 날린 적 있음.
- 지금은 **병원 하나 끝날 때마다 즉시 `data/nonbenefit-prices.ts`에 저장**하고,
  `data/_progress-<구>.json`에 처리 완료한 ykiho를 기록해서, 스크립트가 죽었다 재시작해도
  이미 끝낸 병원은 건너뛰고 이어서 진행한다. **이 로직 건드리지 말 것.**

**3. 절대 같은 fetch 프로세스를 동시에 두 개 띄우지 말 것.**
- opencode에 작업을 맡겼다가 죽이고, 별도로 내가 테스트를 또 띄웠더니 opencode가
  "죽었네, 다시 실행해야지" 하고 자기 판단으로 재실행해서 두 프로세스가 동시에 같은
  파일에 쓰던 상황이 있었다. 다행히 둘 다 병원 목록 스캔 단계에서 죽어서 파일 손상은
  없었음.
- **opencode에 백그라운드 작업을 맡길 땐, 죽었는지 확인할 때 `tasklist //FI "IMAGENAME eq
  node.exe"`와 `tasklist | grep opencode`로 실제 프로세스를 확인하고, 재실행 전엔 반드시
  기존 프로세스를 완전히 죽였는지(`taskkill //PID <pid> //F`, git-bash의 PID는 실제
  Windows PID와 다를 수 있어서 `wmic process where "name='node.exe'" get
  ProcessId,CommandLine`로 실제 PID 확인 필요) 검증할 것.**

**4. 병원 목록 페이지네이션이 정렬 없이 돈다 — 같은 병원이 여러 페이지에서 또 나올 수 있음.**
- `findHospitalsInDistrict()`에서 ykiho 기준으로 이미 중복 제거하도록 고쳐져 있음
  (`seenYkiho` Set). **이것도 이미 고쳐져 있으니 또 만들지 말 것.**

**5. 강남구 실제 매칭 기관 수가 처음 측정한 476보다 훨씬 많을 수 있다.**
- 페이지를 넘길수록 중복 제거 전 누적 매칭 수가 476을 넘어 771까지 올라가는 걸 확인함
  (중복 제거 적용 전 관찰치). 중복 제거 후 실제 숫자가 얼마나 될지는 아직 확인 못 했다 —
  이번 실행에서 처음 몇 페이지 로그를 보고 "중복 제외" 표시된 숫자가 안정적으로
  수렴하는지 확인할 것. 계속 큰 폭으로 늘어나면 뭔가 더 이상한 것이니 멈추고 살펴볼 것.

**6. 실데이터의 정상적인 특성 (버그 아님, 확인된 것):**
- 의원급 클리닉 중 상당수(성형외과·피부과 포함)가 "신고된 비급여 항목 없음"으로 나옴 —
  실제로 소규모 의원의 HIRA 신고 준수율이 낮아서 그렇다. 버그 아님.
- 같은 병원·같은 항목명인데 가격이 다른 레코드가 있을 수 있음 (예: "당뇨병교육"이
  개인/그룹으로 나뉘어 131,000원/56,000원 둘 다 존재) — 세부 상품이 달라서 그런 거고
  중복이 아니다. `npayKorNm`(표시명)은 같아도 실제로는 다른 청구 항목.

**7. Vercel 배포는 사용자가 명시적으로 요청할 때만 push할 것.**
- "로컬만", "vercel은 요청시에만" 이라는 지시가 있었음. 오늘 작업분은 이미 push해서
  라이브 URL에 반영되어 있고 사용자가 직접 확인 완료함 (2026-08-24 기준 정상 동작 확인).

**8. opencode 사용법 (재확인):**
- 무료 모델 중 `opencode/big-pickle`은 한 번 다운돼서 `opencode/nemotron-3-ultra-free`로
  바꿔서 성공한 적 있음. 모델 하나가 안 되면 다른 free 모델로 바꿔서 재시도.
- 명령 예시: `opencode run "..." --dir "C:/Users/USER/Desktop/healthcost" -m
  opencode/nemotron-3-ultra-free --agent build -f "<스펙파일>" --print-logs --log-level
  DEBUG` (백그라운드 실행 권장, run_in_background:true)
- 스펙 파일에 "PLAN 문서 만들지 마라", "git commit/push 하지 마라"를 명시해도 opencode가
  가끔 `PLAN_simple.md` 같은 걸 자기 마음대로 만든 적 있음 — 작업 끝나면 `git status`로
  스펙에 없는 파일이 생겼는지 항상 확인하고 지울 것.

## 지금 저장소 상태
- `main` 브랜치, 로컬/원격 동기화됨, working tree clean.
- 데이터: `data/nonbenefit-prices.ts` (강남구 681 + 서초구 580 + 송파구 526 = 1787건)
- 스크립트: `scripts/fetch-hira-data.mjs` (사용법: `node scripts/fetch-hira-data.mjs [--all]
  <구1> <구2> ...`, `--all` 없으면 구당 8곳 캡)
- 지역 데이터: `data/regions.ts` (시/도 16개 → 시/군/구 262개 → 동/읍/면 4415개, 법정동코드
  API 기반)
- `.env.local`의 `HIRA_API_KEY`는 이미 채워져 있음 (일반인증키, 공공데이터포털 계정 공통).

## 시작할 때 할 일
1. 이 파일 다 읽었으면 `git log --oneline -15`로 최근 커밋들 훑어보기 (커밋 메시지에 각
   수정 이유가 자세히 적혀 있음).
2. `node scripts/fetch-hira-data.mjs --all 강남구` 직접 실행하거나 opencode에 위임.
3. 완료되면 `npx tsc --noEmit`, `npx vitest run`으로 검증 후 커밋. Vercel push는 사용자가
   요청할 때만.
